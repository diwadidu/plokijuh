

import { None, Some, Option } from './Option';
import { version } from '../package.json';

export default {
  version: version,
  Option: Option,
  None: None,
  Some: Some,
};

export {
  version,
  Option,
  None,
  Some,
};
